export default function CustomRating({src, setRating}) {
    return (
        <li onClick={() => {
            
        }}>
            <img
                src={src}
                alt="rating" />
        </li>
    );
};